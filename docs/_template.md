## 函数

参数:

- **arg1** `type`
- **arg2** `type|type`
- **arg3** `type|type|type`

返回值: `表`, `布尔型`

这里会有一个对这个特定函数的简短描述,解释它是做什么的,及其用途.

```lua
local t, b = ni.function("arg1", "arg2", "arg3")

if b then
  --如果b为turn,则执行
end
```
